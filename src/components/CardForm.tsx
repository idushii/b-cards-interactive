import * as tsx from 'vue-tsx-support'
import { VNode } from 'vue'

const CardForm = tsx.component({
  name: 'CardForm',

  render(): VNode {
    return (
      <div>CardForm</div>
    )
  }
})

export { CardForm }
